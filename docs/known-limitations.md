# Known limitations

The packaged adaptive artifact is a development baseline and must be replaced with an approved trained artifact before public release. The current UI has metadata-first memories and a text-first voice fallback. Speech recognition is intentionally not a prerequisite and needs a native development-build decision. Local reminder seeding is a demo-safe placeholder until live reminder data is bootstrapped through verified RLS access. No screen makes clinical claims.
