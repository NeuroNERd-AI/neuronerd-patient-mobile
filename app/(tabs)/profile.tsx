import { SafeAreaView, ScrollView, Text } from 'react-native'; import { Button, Card, Heading, StatusPill } from '../../src/components/ui'; import { styles } from '../../src/theme/tokens'; import { useAuth } from '../_layout'; import { signOut } from '../../src/data/remote/patientRepository';
export default function Profile() { const { profile, setProfile } = useAuth(); async function logout() { await signOut(); setProfile(null); } return <SafeAreaView style={styles.screen}><ScrollView contentContainerStyle={styles.content}><Heading subtitle="Make NeuroNERd comfortable for you.">Profile</Heading><Card><Text style={{ fontSize: 22, fontWeight: '800' }}>{profile?.displayName ?? 'Friend'}</Text><StatusPill>{profile?.locale?.toUpperCase() ?? 'EN'}</StatusPill><Text style={{ color: '#71685D' }}>Your information stays protected by your account and patient access rules.</Text></Card><Card><Heading>Settings</Heading><Button title="Large text" variant="secondary" /><Button title="High contrast" variant="secondary" /><Button title="Sound and voice" variant="secondary" /><Button title="Help and about" variant="quiet" /></Card><Button title="Sign out" variant="danger" onPress={logout} /></ScrollView></SafeAreaView>; }
