# Testing

Run `npm run typecheck` and `npm test`. The unit suite covers adaptive model schema/fallback behavior. Before release, add Android device checks for first sign-in, session restoration, airplane-mode games, notification permissions, process restart, outbox retry, duplicate idempotency keys, sign-out clearing, TalkBack, large text, high contrast, and long Hindi/Assamese/Bengali strings.
