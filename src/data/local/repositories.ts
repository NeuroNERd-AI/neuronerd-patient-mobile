import { getDatabase } from './database';
import type { AppPreferences, GameResult, Memory, Reminder } from '../../types';
import type { DifficultyRecommendation } from '../../domain/adaptive/types';
import { adaptiveRecommendationRow } from '../../domain/adaptive/records';
import { buildReminderEventPayload } from '../../domain/sync/contracts';
const id = (prefix: string) => `${prefix}_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;
export async function createGameSession(gameKey: GameResult['gameKey'], difficulty: GameResult['difficulty']) { const db = await getDatabase(); const sessionId = id('session'); const clientEventId = id('session_event'); const startedAt = new Date().toISOString(); await db.runAsync('INSERT INTO local_game_sessions (session_id, client_event_id, game_key, difficulty, started_at, status, sync_state) VALUES (?, ?, ?, ?, ?, ?, ?)', sessionId, clientEventId, gameKey, difficulty, startedAt, 'started', 'local'); return { sessionId, clientEventId, startedAt }; }
export async function saveGameResult(result: GameResult) { const db = await getDatabase(); await db.withTransactionAsync(async () => { await db.runAsync('INSERT OR REPLACE INTO local_game_results (session_id, client_event_id, game_key, difficulty, score, max_score, accuracy, attempts, duration_ms, completed, metrics_json, created_at, sync_state) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)', result.sessionId, result.clientEventId, result.gameKey, result.difficulty, result.score, result.maxScore, result.accuracy, result.attempts, result.durationMs, result.completed ? 1 : 0, JSON.stringify(result.metrics), result.createdAt, result.syncState); await db.runAsync("UPDATE local_game_sessions SET status = ?, sync_state = ? WHERE session_id = ?", result.completed ? 'completed' : 'abandoned', result.syncState, result.sessionId); await db.runAsync('INSERT OR IGNORE INTO sync_outbox (id, entity_type, operation, entity_id, idempotency_key, payload_json, next_attempt_at) VALUES (?, ?, ?, ?, ?, ?, ?)', result.clientEventId, 'game_result', 'create', result.sessionId, result.clientEventId, JSON.stringify(result), result.createdAt); }); }
export async function logAdaptiveRecommendation(gameKey: GameResult['gameKey'], currentDifficulty: GameResult['difficulty'], recommendation: DifficultyRecommendation) { const db = await getDatabase(); const row = adaptiveRecommendationRow(gameKey, currentDifficulty, recommendation); await db.runAsync('INSERT OR REPLACE INTO adaptive_recommendations (id, game_key, engine, engine_version, feature_schema_version, current_difficulty, recommended_difficulty, predicted_probability, confidence, fallback_reason, created_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)', row.id, row.gameKey, row.engine, row.engineVersion, row.featureSchemaVersion, row.currentDifficulty, row.recommendedDifficulty, row.predictedProbability, row.confidence, row.fallbackReason, row.createdAt); }
export async function completeReminderOffline(reminderId: string, scheduledFor: string) { const db = await getDatabase(); const eventId = id('reminder_event'); const completedAt = new Date().toISOString(); const payload = buildReminderEventPayload(reminderId, scheduledFor, completedAt, eventId); await db.withTransactionAsync(async () => { await db.runAsync("UPDATE local_reminders SET completed_today = 1, sync_state = 'queued' WHERE id = ?", reminderId); await db.runAsync('INSERT INTO local_reminder_events (id, client_event_id, reminder_id, scheduled_for, status, completed_at, sync_state) VALUES (?, ?, ?, ?, ?, ?, ?)', eventId, eventId, reminderId, scheduledFor, 'delivered', completedAt, 'queued'); await db.runAsync('INSERT OR IGNORE INTO sync_outbox (id, entity_type, operation, entity_id, idempotency_key, payload_json, next_attempt_at) VALUES (?, ?, ?, ?, ?, ?, ?)', eventId, 'reminder_event', 'create', eventId, eventId, JSON.stringify(payload), completedAt); }); return eventId; }
export async function undoReminderOffline(reminderId: string) { const db = await getDatabase(); await db.withTransactionAsync(async () => { await db.runAsync("UPDATE local_reminders SET completed_today = 0, sync_state = 'queued' WHERE id = ?", reminderId); }); }
export async function listGameResults() { const db = await getDatabase(); return db.getAllAsync<GameResult>('SELECT session_id as sessionId, client_event_id as clientEventId, game_key as gameKey, difficulty, score, max_score as maxScore, accuracy, attempts, duration_ms as durationMs, completed, metrics_json as metrics, created_at as createdAt, sync_state as syncState FROM local_game_results ORDER BY created_at DESC LIMIT 20'); }
export async function saveMemory(memory: Memory) { const db = await getDatabase(); await db.runAsync('INSERT OR REPLACE INTO local_memories (id, title, description, memory_type, created_at, sync_state) VALUES (?, ?, ?, ?, ?, ?)', memory.id, memory.title, memory.description, memory.type, memory.createdAt, memory.syncState); }
export async function listMemories() { const db = await getDatabase(); return db.getAllAsync<Memory>('SELECT id, title, description, memory_type as type, created_at as createdAt, sync_state as syncState FROM local_memories ORDER BY created_at DESC'); }
export async function seedReminders() { const db = await getDatabase(); const count = await db.getFirstAsync<{ count: number }>('SELECT count(*) as count FROM local_reminders'); if (!count?.count) await db.runAsync('INSERT INTO local_reminders (id, reminder_key, title, message, start_at, timezone, category) VALUES (?, ?, ?, ?, ?, ?, ?)', 'demo-reminder', 'morning-water', 'Drink some water', 'A small glass is a good start.', new Date().toISOString(), Intl.DateTimeFormat().resolvedOptions().timeZone, 'hydration'); }
export async function listReminders() { const db = await getDatabase(); return db.getAllAsync<Reminder>('SELECT id, reminder_key as key, title, message, start_at as startAt, timezone, category, is_active as isActive, completed_today as completedToday, sync_state as syncState FROM local_reminders ORDER BY start_at'); }
export async function getPreferences(): Promise<AppPreferences> { const db = await getDatabase(); const rows = await db.getAllAsync<{ key: string; value: string }>("SELECT key, value FROM app_state WHERE key LIKE 'pref_%'"); const values = Object.fromEntries(rows.map((row) => [row.key, row.value])); return { locale: (values.pref_locale as AppPreferences['locale']) ?? 'en', theme: (values.pref_theme as AppPreferences['theme']) ?? 'default', sound: values.pref_sound !== 'false', notifications: values.pref_notifications !== 'false' }; }
export async function savePreferences(preferences: Partial<AppPreferences>) { const db = await getDatabase(); await db.withTransactionAsync(async () => { for (const [key, value] of Object.entries(preferences)) await db.runAsync('INSERT OR REPLACE INTO app_state (key, value) VALUES (?, ?)', `pref_${key}`, String(value)); }); }
