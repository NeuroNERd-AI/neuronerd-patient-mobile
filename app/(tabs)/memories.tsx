import { useEffect, useState } from 'react'; import { SafeAreaView, ScrollView, Text } from 'react-native'; import { Card, Heading, Button } from '../../src/components/ui'; import { listMemories } from '../../src/data/local/repositories'; import type { Memory } from '../../src/types'; import { styles } from '../../src/theme/tokens';
export default function Memories() { const [items, setItems] = useState<Memory[]>([]); useEffect(() => { listMemories().then(setItems); }, []); return <SafeAreaView style={styles.screen}><ScrollView contentContainerStyle={styles.content}><Heading subtitle="Keep the moments that matter close.">Memories</Heading><Button title="Add a memory" />{items.map((item) => <Card key={item.id}><Text style={{ fontSize: 20, fontWeight: '800' }}>{item.title}</Text><Text>{item.description}</Text></Card>)}{!items.length ? <Card><Text>Your memories will appear here.</Text><Text>Text notes work offline; photos can be added after storage access is verified.</Text></Card> : null}</ScrollView></SafeAreaView>; }
